package com.optimhire.test;

import java.util.LinkedList;

public class Reverse {

	public static void main(String[] args) {
		LinkedList list=new LinkedList();
	}

}

listAlert.stream().collect(Collectors.groupingBy(Allert::getId, ))sala

Slecet * from emp order by sal row-3;

select * from emp where max(sal) as salry<slect max(sal) from emp where salary<select max(sal) from emp;
